{
  "name": "tesla-hw4-extension",
  "version": "0.1.0",
  "private": true,
  "description": "Chrome extension that estimates HW3/HW4 on Tesla pre-owned listings from the VIN.",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2"
  }
}
